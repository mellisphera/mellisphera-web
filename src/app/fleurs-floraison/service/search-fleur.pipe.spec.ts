import { SearchFleurPipe } from './search-fleur.pipe';

describe('SearchFleurPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFleurPipe();
    expect(pipe).toBeTruthy();
  });
});
