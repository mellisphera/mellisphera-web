export interface CurrentIndex{
    _id : string,
    apiaryId: string,
    date: Date,
    flightIdx: number,
    nectarIdx: number,
    sensorType: string,
    sensorRef: string,
    origin: string
}