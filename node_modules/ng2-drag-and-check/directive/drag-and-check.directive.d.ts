import { ElementRef, EventEmitter, OnInit, Renderer } from '@angular/core';
export declare class DragAndCheckDirective implements OnInit {
    private el;
    private renderer;
    private allowDrag;
    private moving;
    private original;
    private oldTrans;
    private tempTrans;
    private currentTrans;
    private oldZIndex;
    private oldPosition;
    started: EventEmitter<any>;
    stopped: EventEmitter<any>;
    edge: EventEmitter<any>;
    handle: HTMLElement;
    bounds: HTMLElement;
    allowedOffsets: Offsets;
    ngDraggable: any;
    constructor(el: ElementRef, renderer: Renderer);
    ngOnInit(): void;
    private getPosition(x, y);
    moveTo(x: number, y: number): void;
    verifyMoveOnX(): void;
    verifyMoveOnY(): void;
    pickUp(): void;
    private boundsCheck();
    putBack(): void;
    onMouseDown(event: any): void;
    onMouseUp(): void;
    onMouseLeave(): void;
    onMouseMove(event: any): void;
    onTouchEnd(): void;
    onTouchStart(event: any): void;
    onTouchMove(event: any): void;
}
export declare class Offsets {
    private top;
    private right;
    private bottom;
    private left;
    static NONE: string;
    static BORDER: string;
    static HALF_WIDTH: string;
    static HANDLE_HEIGHT: string;
    constructor(top?: any, right?: any, bottom?: any, left?: any);
    getTop(el: HTMLElement, handle?: HTMLElement): number;
    getRight(el: HTMLElement, handle?: HTMLElement): number;
    getBottom(el: HTMLElement, handle?: HTMLElement): number;
    getLeft(el: HTMLElement, handle?: HTMLElement): number;
    computeOffset(value: any, el: HTMLElement, handle: HTMLElement): number;
    private computeHALF(el);
    private computeHANDLE(el, handle);
}
